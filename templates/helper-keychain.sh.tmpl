#!/bin/sh
# Generated by claude-profiles — do not hand-edit
_val="$(security find-generic-password -w -s '{{SERVICE}}' -a '{{ACCOUNT}}' 2>/dev/null)"
_rc=$?
if [ "$_rc" -ne 0 ] || [ -z "$_val" ]; then
  echo "claude-profiles: keychain lookup failed for service '{{SERVICE}}' account '{{ACCOUNT}}'" >&2
  exit 1
fi
printf '%s' "$_val"
